<config>
  <!-- restrict access to MindTouch Dream server, either by apikey, username/password pair, or both -->
  
  <!-- requires called to provide "?apikey=nosecrets" in the uri -->
  <!-- apikey>nosecrets</apikey -->

  <!-- requires caller to authenticate with username/password pair -->
  <username>foo</username>
  <password>bar</password>
</config>
